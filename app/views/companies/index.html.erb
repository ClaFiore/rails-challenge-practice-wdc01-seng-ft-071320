<h1> All companies list </h1>


<ul>
    <% @companies.each do |c| %>
    <li><%= link_to c.name, company_path(c) %></li>
    <% end %>

</ul>


<%= button_to 'Create new company', new_company_path, method: :get %>